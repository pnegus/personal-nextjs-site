/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    basePath: '/pnegus.github.io',
    images: {
        unoptimized: true,
      },
};

export default nextConfig;
